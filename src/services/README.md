# Services

This directory contains server-only business and persistence logic grouped by domain/table. Any operation exposed to a Client Component should be wrapped by a corresponding authenticated Server Action in `src/actions`.
